import React from 'react';
import PlayerCard from './PlayerCard';


const PlayerList = () => {
    return (
        <div>
            <h2>Player List</h2>
            <PlayerCard />
        </div>
    );
};

export default PlayerList
